# Claude Instructions

1. ALWAYS create a new branch `claude/<feature-name>`.
2. NEVER merge to main directly.
3. Push changes and wait for the PR link.
4. Inform the user to check the Netlify Preview link in the PR.
5. Wait for user to comment "approve" before any merge action.
